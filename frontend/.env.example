# En desarrollo: dejar vacío/sin definir. El proxy de Vite (vite.config.ts)
# reenvía /api a http://localhost:5223 (la API .NET corriendo local).
#
# En producción (Vercel): la URL pública de la API (Railway), SIN barra final.
# VITE_API_URL=https://tu-api.up.railway.app
