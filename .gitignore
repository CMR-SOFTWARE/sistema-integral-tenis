# ── Node / frontend ──
node_modules/
dist/

# ── .NET / backend ──
bin/
obj/
*.user

# ── Base de datos local (SQLite): datos, no código. Se regenera con
#    'dotnet ef database update'. JAMÁS al repo. ──
*.db
*.db-shm
*.db-wal

# ── Archivos subidos en desarrollo (fotos de perfil): en producción viven en
#    Supabase Storage. Son datos de prueba, no código. ──
archivos-locales/

# ── Secretos y entorno ──
.env
.env.*
!.env.example

# ── IDE / SO ──
.vs/
.vscode/
Thumbs.db
